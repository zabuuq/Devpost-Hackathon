<!-- Every item MUST use the five-field format below. /build reads each item
     and relies on all five fields being present and consistently formatted.
     The header encodes methodology choices so /build doesn't re-ask. -->

# Build Checklist

## Build Preferences

- **Build mode:** Autonomous
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each checklist item with message: "Complete step N: [title]"
- **Verification:** Yes — summary + in-game verification at checkpoints every 3 items
- **Check-in cadence:** N/A (autonomous mode)

## Checklist

- [x] **1. Project scaffold and Godot project setup**
  Spec ref: `spec.md > File Structure`
  What to build: Create the full Godot 4.6.2 project directory layout. Set up `project.godot` with the correct render settings (Compatibility renderer for HTML5), disable threading in export settings, register `GameState` and `AudioManager` as Autoload singletons. Create all scene and script placeholder files at their correct paths per the file structure in spec.md. No logic yet — stubs only. Confirm the project opens in Godot without errors.
  Acceptance: The project opens in Godot 4.6.2. Scene tree shows root scene. Autoloads appear in Project Settings → Autoloads. No script errors in the output panel.
  Verify: Open the project in Godot. Check Project Settings → Autoloads for GameState and AudioManager. Hit F5 — game runs without crash even if nothing displays.

- [x] **2. Data model — ShipDefinitions, ShipInstance, FogShipRecord, CellRecord**
  Spec ref: `spec.md > Data Model`
  What to build: Write all four data model scripts with full static typing. `ShipDefinitions` (`scripts/data/ship_definitions.gd`) — static `SHIPS` dictionary with all stats for battleship, probe_ship, destroyer, cruiser; `FLEET` constant array. `ShipInstance` (`scripts/gameplay/ship_instance.gd`) — all fields as specified: position, facing, current stats, slider settings, turn state flags. `FogShipRecord` (`scripts/gameplay/fog_ship_record.gd`) — ship_type, position, facing, last_shields, last_armor. `CellRecord` (`scripts/gameplay/cell_record.gd`) — has_probe, expires_in, has_blind_hit, ship (FogShipRecord reference). All classes use `class_name` declarations.
  Acceptance: All four scripts parse without errors. `ShipDefinitions.SHIPS["battleship"].max_shields` returns 1000. `ShipDefinitions.FLEET` returns the 5-ship array with two destroyers.
  Verify: Open each script in Godot. Confirm no parse errors (red squiggles). Use Godot's built-in script runner or a temporary test scene to print `ShipDefinitions.SHIPS["battleship"]` and confirm the dictionary.

- [x] **3. GameState and AudioManager autoloads**
  Spec ref: `spec.md > Autoloads`
  What to build: Implement `GameState` (`scripts/autoloads/game_state.gd`) — Phase enum (SPLASH, MENU, PLACEMENT, HANDOFF, GAMEPLAY, VICTORY), `phase`, `current_player`, `turn_number`, `last_turn_hits`, and the `players` array with fleet and cell_records sub-dictionaries shaped exactly as spec'd. Implement `AudioManager` (`scripts/autoloads/audio_manager.gd`) — `play_sfx(name)`, `play_music(name)`, `stop_music()`, `set_sfx_enabled(enabled)`, `set_music_enabled(enabled)`. AudioManager reads sfx_enabled and music_enabled from GameState. Both singletons fully typed.
  Acceptance: GameState and AudioManager load without errors. `GameState.players` has two entries with correct shape. `GameState.phase` defaults to SPLASH. `AudioManager.set_sfx_enabled(false)` does not throw an error.
  Verify: Run the project. In Godot's Remote inspector (while running), expand the Autoloads and confirm GameState and AudioManager nodes exist with their exported properties visible.

<!-- CHECKPOINT 1 after item 3: Run the project. Confirm no errors in Output. Check Autoloads in Remote inspector. -->

- [x] **4. Splash, Main Menu, and Handoff scenes**
  Spec ref: `spec.md > Scenes > Splash Screen`, `spec.md > Scenes > Main Menu`, `spec.md > Scenes > Handoff Screen`
  What to build: Build three scenes with full UI and logic. `splash.tscn` — nebula background, game name label, "Press any key to load game" prompt; on any input event call `AudioServer.unlock()` and transition to `main_menu.tscn`. `main_menu.tscn` — Start Game button (→ fleet_placement.tscn, sets current_player=0), How to Play button (opens in-screen overlay), Sound toggle, Music toggle; ambient music starts here. `handoff.tscn` — "Player X, your turn. You took N hits. Click Next." text built from GameState; Next button routes to correct next scene based on phase (placement or gameplay). All buttons play click SFX via AudioManager.
  Acceptance: Clicking through Splash → Main Menu → How to Play overlay → back → Start → Handoff works end-to-end. Hit count on handoff reads from GameState.last_turn_hits. Music toggle silences/resumes music.
  Verify: Run the project. Click through the full flow: splash → main menu → How to Play → close → Start Game → handoff screen. Confirm "Player 1, your turn" displays. Confirm music plays on main menu and toggles off/on.

- [x] **5. Fleet Placement scene**
  Spec ref: `spec.md > Scenes > Fleet Placement`, `prd.md > 2.2–2.3 Fleet Placement`, `prd.md > 8. Fleet Placement Rules`
  What to build: Build `fleet_placement.tscn` with three-panel layout. Left panel: ship list showing all 5 ships as horizontal strips (squares to scale); click to select a ship for placement. Center: 80×20 grid using SubViewport + Camera2D, nebula background; ghost ship follows cursor while a ship is selected; Q/E to rotate; ghost turns red on invalid placement (overlap or out-of-bounds); click to place. Right panel: ship detail card — name, illustration placeholder, special ability, stats from ShipDefinitions. Done button disabled until all 5 ships placed; on click, writes placed fleet to `GameState.players[current_player].fleet` and loads `handoff.tscn`. Reused for both players via `GameState.current_player`.
  Acceptance: All 5 ships can be placed on the grid. Rotation works with Q/E. Overlap and out-of-bounds turns ghost red. Done button activates only when all 5 are placed. Ship detail panel updates on selection. Fleet data written correctly to GameState.
  Verify: Run to fleet placement. Place all 5 ships. Attempt to overlap two ships — confirm red ghost. Rotate a ship with Q/E. Click Done. Confirm handoff screen loads for Player 2.
  Status note: The scene and script exist (`fleet_placement.tscn` / `scripts/fleet_placement.gd`) and most logic works — ship list, rotation, overlap detection, Done button, GameState write. Two bugs found and not yet fixed: (1) ghost ship renders offset from cursor — coordinate conversion in `_screen_to_grid()` is wrong; 4 fix attempts made, see memory note for hypotheses. (2) scroll wheel zoom and middle-mouse pan are missing from `_on_viewport_gui_input` — the zoom/clamp helpers in `gameplay.gd` should be ported here. Read the memory file `project_mouse_tracking_bug.md` before touching coordinate math.

- [x] **6. Gameplay screen layout and SubViewport grid setup**
  Spec ref: `spec.md > Scenes > Gameplay Screen`, `spec.md > Grid Rendering > SubViewport Setup`
  What to build: Build `gameplay.tscn` layout — top tab row (Command Grid / Target Grid buttons), left panel with Battle Log and Ship Panel tabs, center SubViewportContainer. Create two SubViewport instances each containing a Node2D (GridRenderer stub) and Camera2D. Wire grid tab buttons to switch the active SubViewport. Camera2D zoom limits: full zoom-out shows entire 80×20 grid; max zoom ~4× cell size. Pan by click-and-drag on empty grid space. Ship Panel tab auto-focuses when a ship is clicked; Battle Log tab auto-focuses when nothing is selected. `gameplay.gd` calls `TurnManager.turn_start()` on scene load.
  Acceptance: Gameplay scene loads after the second handoff. Both grid tabs are clickable and switch the visible SubViewport. Zoom in/out works with scroll wheel. Pan works on empty grid space. Left panel tabs switch between Battle Log and Ship Panel.
  Verify: Play through fleet placement for both players. Confirm gameplay scene loads. Click Command Grid / Target Grid tabs. Scroll to zoom in and out. Drag to pan. Click a ship — confirm Ship Panel tab activates.

<!-- CHECKPOINT 2 after item 6: Full flow from splash → placement (both players) → gameplay screen. Both grids switchable. Zoom/pan working. -->

- [x] **7. GridRenderer — Command Grid and Target Grid rendering**
  Spec ref: `spec.md > Grid Rendering > GridRenderer`
  What to build: Implement `grid_renderer.gd` attached to the Node2D inside each SubViewport. Rendering layers in order: (1) nebula background texture tiled to 80×20 bounds, (2) grid cell lines, (3) wreckage markers, (4) ships — Command Grid: full color with facing indicator; Target Grid: reads CellRecord state for each cell and renders clear ship / ghost ship / blind hit marker / ghost marker accordingly, (5) probe illumination overlay (semi-transparent highlight on probed cells, nebula still visible beneath), (6) targeting reticule during action targeting (4×4 or 6×6 highlight clamped to grid bounds, moves with mouse). Use placeholder colored rectangles for ships if art assets are not yet ready. Probe area highlight renders in real time during probe targeting mode.
  Acceptance: Command Grid shows all placed ships with facing arrows. Target Grid shows only fog-of-war state (no ships visible unless probe data exists). Ghost markers render semi-transparent. Probe area highlight tracks mouse and clamps at grid edges.
  Verify: In gameplay, confirm your own ships are visible on Command Grid. Target Grid shows blank fog. Launch a probe — confirm illuminated area appears over nebula background. Move cursor near grid edge — confirm probe highlight clamps correctly.

- [x] **8. TurnManager — turn sequence, probe aging, energy, shield regen, win condition**
  Spec ref: `spec.md > Turn Manager`, `spec.md > Data Model > CellRecord > Probe age logic`
  What to build: Implement `turn_manager.gd` with `turn_start()` and `turn_end()`. `turn_start()`: call `age_cell_records()` on active player's cell_records (decrement expires_in, convert expired probes to ghost markers, delete empty expired cells), regen +50 energy per living ship, reset `action_taken` and `move_actions_taken` per ship, recalculate slider settings vs available energy (shields fill first). `turn_end()`: fire shield regen per ship's shield_regen_setting (deduct energy), check win condition (all opponent ships destroyed → load victory.tscn), otherwise swap current_player, write last_turn_hits, load handoff.tscn. `fire_shield_regen()` and `check_win_condition()` implemented exactly as spec'd.
  Acceptance: Energy regenerates at turn start. Probe intel ages correctly — standard probe fades after 2 of your turns, Probe Ship probe after 3. Shield regen fires at turn end and deducts energy. Destroying all opponent ships triggers victory screen.
  Verify: Play a turn. Confirm energy increases by 50 at turn start. Launch a probe; skip 2 turns; confirm probe fades to ghost marker. Set shield regen slider; end turn; confirm shields increase and energy decreases. (Win condition verified during ActionResolver step.)

- [x] **9. ActionResolver — Probe, Laser, and Missile actions**
  Spec ref: `spec.md > Action System > ActionResolver > Probe Action`, `spec.md > Action System > ActionResolver > Laser Action`, `spec.md > Action System > ActionResolver > Missile Action`
  What to build: Implement `action_resolver.gd` with `resolve_probe()`, `resolve_laser()`, `resolve_missile()`. Probe: determine area (4×4 or 6×6), clamp to grid bounds, deduct energy, decrement probes_remaining, write CellRecords with correct expires_in, handle overlap reset, return ProbeResult for battle log. Laser: deduct laser_power_setting energy, check hit/miss, apply shield damage first then 75% overflow to armor, set blind hit on CellRecord if no active probe, increment hits_scored and last_turn_hits. Missile: decrement missiles_remaining, check hit/miss, 125 damage to shields + full overflow to armor (no percentage), 250 armor if shields are gone. Ship destruction: armor ≤ 0 sets is_destroyed, leaves wreckage. All results written to GameState and returned for battle log.
  Acceptance: Probe reveals ships in area and creates CellRecords. Laser strips shields then damages armor at 75% overflow. Missile does 250 armor / 125 shield as specified. Destroying a ship (armor to 0) marks it destroyed. Blind hit marker appears on Target Grid when firing without probe coverage.
  Verify: Launch a probe over an enemy ship — confirm ship appears on Target Grid. Fire a laser at a probed ship — confirm shield/armor values decrease correctly. Fire a missile — confirm flat 250/125 damage. Fire at a ship until armor hits 0 — confirm wreckage marker appears.
  Status: **CODE COMPLETE — TESTING IN PROGRESS.** Bugs found and fixed: (1) action_taken not set consistently across probe/laser/missile — centralized into each resolver method. (2) Probe records not updating when enemy ship moves into/out of/within active probe — added `_update_opponent_probes_after_move`. (3) Probe records not updating shield/armor after laser/missile damage — added `_refresh_probe_records_for_ship`. (4) Laser power defaulted to 0 instead of ship's laser_strength — fixed in ShipInstance.create.

<!-- CHECKPOINT 3 after item 9: Full game loop playable. Both players can probe, fire, take turns. Fog of war updates correctly. Win condition triggers. -->

- [x] **10. ActionResolver — Move action**
  Spec ref: `spec.md > Action System > ActionResolver > Move Action`, `prd.md > 7. Movement`
  What to build: Implement `resolve_move()` in `action_resolver.gd`. Move UX: player clicks Move → ship enters preview mode (ghost ship shows proposed position, live display of move points and energy cost). WASD = screen-relative movement (W=up, S=down, A=left, D=right). Q/E = rotate (1.0 move points / 50 energy, max 1 rotation per move action). Forward move (facing direction) = 0.5 pts / 25 energy; all other directions = 1.0 pts / 50 energy. Cost calculated from net displacement from origin (not cumulative path). Available move points = min(base_move_points, floor(energy/25)*0.5). Submit button → "Are you sure?" confirmation → execute move. Collision: living ships block movement (show message, do not consume action point); wreckage is passthrough. Cruiser: 2 move actions per turn (move_actions_taken tracked).
  Acceptance: Ships move correctly with WASD. Rotation changes facing without moving. Forward move costs 0.5 pts, all other directions 1.0 pts. Cruiser can move twice. Collision with a living ship shows message and does not consume the action. Ghost preview updates live with cost display.
  Verify: Select a ship and click Move. Move it around in preview mode — confirm ghost updates and cost display changes. Submit — confirm ship moves. Try moving into another ship — confirm blocked message. Test Cruiser's double move. Confirm rotation changes facing correctly.
  Status: **CODE COMPLETE — TESTING IN PROGRESS.** Bugs found and fixed: (1) Move cost used origin displacement instead of pivot displacement — rotation-induced origin shifts were counted as movement. Fixed to use pivot points. (2) Cruiser had "double_move" special giving 2 move actions — corrected to just have 2.0 base_move_points (more range, not more actions). (3) Rotate-back was blocked by boolean flag — replaced with net-facing-diff allowing undo of rotation. (4) Move button didn't check action_taken — added check. (5) Slider refresh didn't update action buttons — fixed to call full _refresh_display. (6) Move collision checked both fleets — enemy ships are invisible and shouldn't block; now only checks own fleet. (7) Action buttons hidden after viewing enemy ship then selecting own ship — show_ship now restores visibility.

- [x] **11. Battle Log, Victory Screen, audio, and visual polish**
  Spec ref: `spec.md > Battle Log`, `spec.md > Scenes > Victory Screen`, `spec.md > Audio`, `prd.md > 2.6 Victory Screen`, `prd.md > 10. Audio`
  What to build: **Battle Log** (`scripts/ui/battle_log.gd`): scrollable log; three entry tiers (blind fire minimal, active probe full detail with coordinates and damage numbers, move entry); auto-scroll to latest; format exactly as spec'd. **Victory Screen** (`scenes/victory.tscn`): winner announcement, per-player stats side by side (probes launched, total hits scored), Play Again → main_menu.tscn. **Audio**: wire all SFX in AudioManager (laser.ogg, missile.ogg, probe.ogg, explosion.ogg, hit.ogg, click.ogg, ambient_space.ogg); use royalty-free placeholder audio files if originals not yet sourced; call correct `play_sfx()` from each action. **Visual polish**: nebula background texture applied to all grids; ship portrait placeholders in Ship Panel (colored rectangles with ship type label); probe illumination overlay rendering correctly over nebula; UI panels styled with space theme (dark backgrounds, teal/blue accent colors).
  Acceptance: Battle log shows correct tier detail per action. Victory screen displays with correct winner and stats. All SFX fire on correct events. Music plays on main menu and toggles correctly. Nebula background visible on grids with probe illumination overlaid (not replacing it).
  Verify: Play a full game to completion. Read the battle log — confirm correct detail tiers for probe, laser hit with/without probe, and move. Destroy all opponent ships — confirm victory screen loads with correct stats. Confirm laser, missile, probe, hit, explosion SFX play on corresponding actions.
  Status: **CODE COMPLETE — TESTING IN PROGRESS.** Bugs found and fixed: (1) Opponent's last-turn results not shown in battle log — added replay of GameState.last_turn_results at scene load. (2) Clicking probed enemy ship on target grid did nothing — added _try_select_enemy_ship and show_enemy_ship on ship panel. (3) No visual distinction for probed areas on target grid — added probe illumination overlay pass. (4) Facing indicator was a dot on the rear cell — redesigned as elongated triangle on the front cell.

## Iteration 1 — Audio SFX

- [x] **I1-1. Install numpy for audio synthesis**
  Spec ref: New — not in original spec
  What to build: Run `pip install numpy` to install the numpy package. Confirm it imports successfully in Python.
  Acceptance: `python -c "import numpy; print(numpy.__version__)"` prints a version number without error.
  Verify: Run the acceptance command above.

- [x] **I1-2. Generate 6 SFX audio files with Python + numpy**
  Spec ref: `spec.md > Audio`
  What to build: Write a Python script (`tools/generate_sfx.py`) that procedurally generates 6 sci-fi sound effects using numpy waveform synthesis: (1) `laser.ogg` — short high-frequency sweep downward, (2) `missile.ogg` — rising whoosh with low rumble, (3) `probe.ogg` — electronic ping/sonar pulse, (4) `explosion.ogg` — noise burst with decay, (5) `hit.ogg` — short metallic impact, (6) `click.ogg` — crisp UI click. Script outputs `.wav` files to a temp directory, then converts each to `.ogg` using ffmpeg. Final `.ogg` files placed in `assets/audio/sfx/`. Each sound should be 0.2–1.5 seconds, 44100 Hz sample rate, mono. Sounds should feel sci-fi and space-themed — not realistic, but satisfying.
  Acceptance: All 6 `.ogg` files exist in `assets/audio/sfx/`. Each file plays audible, distinct sound. No clipping or distortion.
  Verify: Play each `.ogg` file manually. Confirm each sounds distinct and appropriate for its action.

- [x] **I1-3. Verify SFX playback in-game**
  Spec ref: `spec.md > Audio`, `prd.md > 10. Audio`
  What to build: Run the game and trigger each sound effect: click a button (click.ogg), launch a probe (probe.ogg), fire a laser (laser.ogg), fire a missile (missile.ogg), hit an enemy ship (hit.ogg), destroy an enemy ship (explosion.ogg). Confirm AudioManager plays each at appropriate volume without overlap issues. Adjust volumes in the generation script if any sound is too loud/quiet relative to others, regenerate, and re-test.
  Acceptance: All 6 SFX fire on their correct game events. No SFX is jarring, silent, or clipping. Volume levels feel balanced relative to each other.
  Verify: Play through a partial game triggering all 6 actions. Confirm each sound plays.

## Iteration 2 — Submission Content & In-Game Tutorial

- [x] **I2-1. Export to HTML5 and deploy to itch.io via butler**
  Spec ref: `spec.md > Runtime & Deployment`, CLAUDE.md > Build & Deploy
  What to build: Export the current project to HTML5 using the existing export preset. Confirm threading is disabled. Output to `export/web/`. Verify the exported build loads and plays in a local browser before pushing. Push to itch.io via `butler push ./export/web <username>/<game-slug>:html5`. Confirm the live page loads and the game reaches the main menu. If any step fails (export error, local load failure, butler push error, live page load failure), halt the iteration and surface the error for a fix pass before continuing. Do not proceed to I2-2 until the live itch.io build works.
  Acceptance: `export/web/` contains a working HTML5 build. Local browser confirms the game loads and reaches the main menu. `butler push` completes without error. Live itch.io URL loads the game and reaches the main menu from a fresh browser tab.
  Verify: Open the live itch.io URL in a fresh browser tab. Confirm splash screen displays, audio unlocks on first input, main menu loads, and the How to Play overlay opens. Record the live URL in the process notes so I2-2 can reference it.

- [x] **I2-2. Create Cowork screenshot brief**
  Spec ref: New (not in original spec)
  What to build: Create `docs/claude-cowork/screenshot-brief.md`. Answer every section in `docs/claude-cowork/needs-for-screenshot-capture.md`. Section 1: the live itch.io URL from I2-1, plus load quirks (WASM load delay, audio unlock on first input). Section 2: game title, one-sentence pitch, genre (turn-based hot-seat tactical), target resolution. Section 3: full controls reference pulled from `scripts/gameplay.gd`, `scripts/fleet_placement.gd`, and `scripts/main_menu.gd` (mouse, keyboard, WASD, Q/E, scroll zoom, middle-mouse pan). Section 4: step-by-step playthrough path from splash through fleet placement for both players to a mid-game state showing an active probe over an enemy ship. Section 5: screenshot plan listing 11+ shots with filenames (pattern `NN_<name>.png`): 8 tutorial shots (welcome/splash, fleet placement, command/target grids, probing, combat, movement, energy, winning) plus 3-5 itch.io shots (hero shot, probe illumination beauty shot, ship panel closeup, victory screen). Section 6: How to Play alignment using the 8-page structure. Section 7: known issues from I2-1 verification (if any). Section 8: reaction time warning not applicable, turn-based. Section 9: audio unlock handling, no cookie banners expected. Section 10: delivery to `assets/screenshots/`. Adhere to `WRITING-STYLE.md`.
  Acceptance: File exists at `docs/claude-cowork/screenshot-brief.md`. Every section from `needs-for-screenshot-capture.md` is addressed. Screenshot plan lists at least 11 shots with clear filenames, target moments, and on-screen state descriptions.
  Verify: Read the brief. Cross-check against `needs-for-screenshot-capture.md` section by section. Confirm the playthrough path is concrete enough for Cowork to follow without guessing.

<!-- PAUSE after I2-2. User hands the screenshot brief to Claude Cowork. Cowork runs in parallel with I2-3 through I2-6. -->

- [x] **I2-3. Create `docs/pitch.md`**
  Spec ref: New (not in original spec)
  What to build: Create `docs/pitch.md` as the source-of-truth content doc. Section 1: elevator pitch, 2-3 sentences. Section 2: tagline candidates, 3-5 options for Devpost and itch.io headers. Section 3: tribute story with the dad/Pascal framing, polished in the voice locked during scoping (dark-playful humor, second person, zero em dashes). Section 4: mechanical hook, the probe fade system framed as a feature with concrete stakes. Section 5: visual hook, nebula background and probe illumination as a feature. Section 6: feature bullets, 6-10 bullets covering hot-seat play, 5-ship fleet, fog-of-war grid, probe/laser/missile/move actions, energy management, probe fade to ghost markers. Section 7: credits including Kenney CC0 audio packs (sci-fi-sounds, impact-sounds, interface-sounds), Godot 4.6.2, GDScript. Voice matches the approved Welcome page draft from the scoping conversation. Adhere to `WRITING-STYLE.md`.
  Acceptance: File exists. All 7 sections present. Zero banned words from `WRITING-STYLE.md`. Zero em dashes. Voice matches the Welcome page draft (second person, confident, direct, dark-playful).
  Verify: Read the file. Grep for banned words. Grep for em dashes. Compare tone against the approved Welcome draft in process notes.

- [x] **I2-4. Create technical `README.md`**
  Spec ref: New (not in original spec)
  What to build: Create `README.md` at project root. Section 1: one-paragraph project description cribbed from `docs/pitch.md`'s elevator pitch and tribute angle. Section 2: Play it live, a link to the itch.io URL from I2-1. Section 3: Setup and run, requires Godot 4.6.2, open the project file, F5 to run. Section 4: Build for web, HTML5 export preset, threading disabled, output to `export/web/`, butler push command. Section 5: File structure overview of `scripts/`, `scenes/`, `assets/`, `docs/`, `tools/`. Section 6: Design docs pointer to the `docs/` folder with one-line descriptions of scope.md, prd.md, spec.md, pitch.md. Section 7: Credits (Kenney CC0 audio, Godot 4.6.2, GDScript). Section 8: License if applicable. Technical tone per user decision. No hero screenshot. Adhere to `WRITING-STYLE.md`.
  Acceptance: `README.md` exists at project root. All sections present. Setup instructions match the actual project. itch.io URL is the real deployed URL from I2-1. Zero banned words, zero em dashes.
  Verify: Read the README. Follow the setup instructions against the project to confirm they work. Click the itch.io link to confirm it loads.

- [x] **I2-5. Create `docs/itch-io-description.md`**
  Spec ref: New (not in original spec)
  What to build: Create `docs/itch-io-description.md` as ready-to-paste content for the itch.io game page Description field. Section 1: tagline at the top. Section 2: hero hook, tribute framing adapted from `docs/pitch.md`. Section 3: how to play summary, condensed from the 8-page tutorial structure. Section 4: feature bullets adapted from pitch.md. Section 5: controls reference for keyboard and mouse. Section 6: credits (Kenney CC0 audio, Godot). Section 7: links including the GitHub repo URL. Use itch.io-compatible markdown (headings, bold, italic, lists, links, images). Reference 3-5 screenshot filenames from `docs/claude-cowork/screenshot-brief.md` that Cowork will upload in I2-6. Adapt from `docs/pitch.md`. Adhere to `WRITING-STYLE.md`.
  Acceptance: File exists. Structured for itch.io paste. Pulls from pitch.md. References screenshot filenames from the Cowork brief. Zero banned words, zero em dashes.
  Verify: Read the file. Confirm structure fits the itch.io Description field format. Confirm every referenced screenshot filename exists in the Cowork brief's screenshot plan.

- [x] **I2-6. Create itch.io page setup brief for Cowork (theme + description upload)**
  Spec ref: New (not in original spec)
  What to build: Create `docs/claude-cowork/itch-page-setup-brief.md` for a second Cowork session. Section 1: live edit URL for the itch.io game page and login access details. Section 2: theme update instructions applying a theme matching the game's nebula aesthetic, dark background (deep space purple/blue/teal), light text, teal/blue accent colors for buttons and links, consistent with the in-game UI. Specify concrete hex colors pulled from `scenes/main_menu.tscn` and `scripts/ui/grid_renderer.gd` where possible. Reference itch.io theme editor sections: background, text, link color, button style. Section 3: description upload instructions to paste the full contents of `docs/itch-io-description.md` into the game page's Description field. Note any markdown conversion needed if itch.io rejects the raw markdown. Section 4: save and verify by re-opening the live game page in a fresh browser tab to confirm theme and description render correctly. Adhere to `WRITING-STYLE.md`.
  Acceptance: File exists at `docs/claude-cowork/itch-page-setup-brief.md`. Theme section specifies concrete hex colors sourced from the project. Description upload section points to `docs/itch-io-description.md`. Verification step is clear and concrete.
  Verify: Read the brief. Confirm a Cowork agent could follow it end to end without guessing.

<!-- PAUSE after I2-6. User hands the page setup brief to Claude Cowork for theme + description upload. -->

- [x] **I2-7. Update checklist item #12 and add HOLD marker**
  Spec ref: `docs/checklist.md > item 12`
  What to build: Edit item #12 in `docs/checklist.md`. Add a note in "What to build" stating that `docs/pitch.md` is the source for the Devpost project name, tagline, project story, and built-with framing, and that `docs/itch-io-description.md` is the already-written itch.io Description (live on itch.io after I2-6). Add a note that the initial deploy happened in I2-1 and the theme + description upload happened in I2-6, so item #12's deploy step becomes a final redeploy of the latest build after I2-8 wires screenshots into the tutorial overlay. Add an HTML comment marker immediately above item #12: `<!-- HOLD — item 12 is the final submission step. Do not /build this item until all iteration work is complete. -->`. Additive edits only. Preserve all original content verbatim.
  Acceptance: Item #12 references both new files. Redeploy note is present. HOLD marker sits directly above item #12. Original item #12 text is not modified outside of the additive note.
  Verify: Read item #12 section. Confirm file references, redeploy note, and HOLD marker are present. Diff against the original to confirm only additions.

- [x] **I2-8. Rewrite "How to Play" as 8-page paged overlay**
  Spec ref: `prd.md > 2.1 Main Menu`, `spec.md > Scenes > Main Menu`
  What to build: Redesign `HowToPlayOverlay` in `scenes/main_menu.tscn`. Replace the single `Label` with a templated paged layout: title label at top, screenshot `TextureRect` (fixed size to prevent layout shift), body `RichTextLabel` with autowrap, a bottom row containing Previous button, page indicator label ("Page X of N"), Next button, Close button. In `scripts/main_menu.gd`: add typed state (`current_page: int`), a typed array of page-data dictionaries (title, screenshot path, body text), handlers for Previous/Next/Close that advance, rewind, clamp, and dismiss, disabled edge buttons at page bounds, click SFX on every button press via AudioManager. Write content for all 8 pages in the voice locked during scoping: (1) Welcome/Objective using the approved Welcome draft, (2) Fleet Placement, (3) Command Grid vs Target Grid, (4) Probing (4×4 standard, 6×6 Probe Ship, fade to ghost markers), (5) Lasers & Missiles (lasers strip shields, missiles kill armor, blind vs probed hits), (6) Movement (WASD screen-relative, Q/E rotate, forward is cheaper, Cruiser moves twice), (7) Energy & Shields (regen at turn start, sliders, shields take priority), (8) Winning (destroy all armor, ghost markers, victory stats). Wire screenshots from `assets/screenshots/` into each page using filenames from the Cowork brief. If Combat and Energy feel cramped as a pair during writing, merge into one page and drop to 7 pages. Before starting this item, verify that Cowork has delivered every required screenshot. Adhere to `WRITING-STYLE.md`.
  Acceptance: Clicking How to Play on main menu opens the paged overlay. Every page renders title, screenshot, and body text. Next and Previous navigate correctly and disable at edges. Page indicator shows current and total page counts. Close dismisses the overlay and returns to the main menu. Voice matches the approved Welcome draft. Zero banned words, zero em dashes. Click SFX plays on button presses. Every page has a valid screenshot wired in.
  Verify: Run the game. Open How to Play. Click through every page in forward order. Click Previous to reverse. Confirm Previous is disabled on page 1 and Next is disabled on the last page. Click Close and confirm return to main menu. Reopen and read every page for voice consistency and readability.

- [x] **I2-9. Rewrite `docs/itch-io-description.md` in the How to Play voice**
  Spec ref: New (not in original spec); voice reference = `HOW_TO_PLAY_PAGES` in `scripts/main_menu.gd`
  What to build: Full rewrite of `docs/itch-io-description.md` in the unhinged-deadpan tutorial voice locked during the How to Play iteration. Preserve structure (H1 title, tagline, tribute hook, `How to play`, `Features`, `Controls`, `Credits`, `Links`). Rewrite register. Match the tutorial's approved pages 1-9 — second person, absurdist sci-fi comedy, zero em dashes, voice consistent with `docs/writing-voice.md`. Keep the same screenshot references (`13_command_overview.png`, `11_probe_closeup.png`, `07_probe_revealed.png`, `09_move_preview.png`). Preserve all links verbatim (secret URL, GitHub repo). Workflow: I draft, user Grammarlies, I apply verbatim (curly→straight quotes normalized only).
  Acceptance: Every section from the original file is present with rewritten copy. Voice matches tutorial pages 1-9. Zero em dashes. Screenshot references unchanged. Live URL and GitHub URL unchanged. Grammarly-polished text applied verbatim.
  Verify: Read the rewritten file end to end against tutorial page 1 ("Welcome, Commander") and page 6 ("Probes Are Flashlights"). Confirm the register matches. Grep for `—`. Grep for every banned word in `docs/writing-voice.md`.

- [x] **I2-10. Create `docs/color-scheme.md` as standalone project palette reference**
  Spec ref: New (not in original spec)
  What to build: Create `docs/color-scheme.md` as the project-wide color reference. Sample every color in use from `scenes/*.tscn`, `scripts/ui/grid_renderer.gd`, and any style resources. For each color document: hex code, RGB if useful, intended role (background / text / accent / ship tint / state marker / etc.), source file and line. Group by function (base palette, UI chrome, ship fleet colors, probe/fog state, combat markers). Include a short header explaining what the doc is and how to extend it. Update `docs/claude-cowork/itch-page-setup-brief.md` Section 2 to reference `docs/color-scheme.md` as the source of truth (keep the itch.io field mapping table inline so Cowork doesn't need to read both docs, but note at the top that the hex values originate from `docs/color-scheme.md`). Adhere to `docs/writing-voice.md`.
  Acceptance: `docs/color-scheme.md` exists. Every color is sourced to a concrete file and line (or marked "convention" where no single source exists). `itch-page-setup-brief.md` points to it without duplicating the full palette. Zero em dashes.
  Verify: Read `docs/color-scheme.md`. Pick three colors at random, open the referenced file:line, confirm the hex matches. Open the brief, confirm the pointer to `docs/color-scheme.md` is present.

- [x] **I2-11. Cowork executes theme + custom description upload on itch.io**
  Spec ref: `docs/claude-cowork/itch-page-setup-brief.md`
  What to build: User drives a fresh Cowork session using the updated `docs/claude-cowork/itch-page-setup-brief.md`. Cowork logs into itch.io (already authenticated), applies the custom theme using the hex codes from `docs/color-scheme.md` (via the brief's Section 2 field map), clears and pastes the rewritten `docs/itch-io-description.md` into the Description field, uploads screenshots to the Screenshots gallery, and saves. The page stays a draft — no visibility flip in this session. Cowork verifies via the secret URL and writes a one-line pass/fail + notes to `docs/claude-cowork/itch-page-setup-delivered.txt`.
  Acceptance: `docs/claude-cowork/itch-page-setup-delivered.txt` exists with a pass marker and the session date. The secret URL renders the custom theme, the rewritten description, and the screenshot gallery. Any Cowork fallbacks (e.g., inline image fallback from brief Section 3) are recorded in the delivered note. Page stays a draft.
  Verify: Open the secret URL from the brief in a fresh browser tab. Confirm background is `#0d0d26`, text is `#9999cc`, the Run game button is `#3366ff`. Confirm every H1/H2/list from the new description renders. Confirm at least five gallery screenshots are visible. Read the delivered note.

---

<!-- HOLD LIFTED 2026-04-22 — all iteration work is complete (I2-9, I2-10, I2-11 done; Cowork confirmed theme + description live on the itch.io draft). User has signed off on proceeding with item 12. This item drives external side effects (butler push, itch.io public flip, Devpost submit), so /build should operate in guided-with-gates mode: pause for explicit user confirmation before each external action, and surface any irreversible step clearly. -->

- [x] **12. HTML5 export, itch.io deploy, and Devpost submission**
  Spec ref: `spec.md > Runtime & Deployment`, `prd.md > 12. Success Criteria`
  What to build: **Export**: in Godot Editor → Project → Export → HTML5 preset. Disable threading. Output to `export/web/`. Run the exported game in a local browser and confirm it plays end-to-end. **Deploy to itch.io**: run `butler push ./export/web <username>/<game-slug>:html5`. On itch.io, confirm the game page is set to HTML (Kind of Project) and the channel is marked "Playable in browser." **GitHub repo**: confirm the repo is public and all code is pushed. **Devpost submission**: write project name and tagline (nebula-themed tribute angle). Draft the project story — the human hook (tribute to dad's Pascal game), the probe fade mechanic as the tactical "wow," the visual artistry (nebula background + probe illumination) as the aesthetic "wow." Add built-with tags: Godot 4, GDScript, HTML5, itch.io. Add screenshots: fleet placement grid, gameplay with active probe illumination over nebula, ship panel with energy sliders, victory screen. Link GitHub repo and itch.io live demo. Submit.
  Iteration 2 notes: `docs/pitch.md` is the source of truth for the Devpost project name, tagline, project story, and built-with framing — crib from it directly rather than rewriting. `docs/itch-io-description.md` was rewritten in the How to Play voice during I2-9 and is live on the itch.io page after I2-11 (Cowork applied the theme using `docs/color-scheme.md` and pasted the rewritten description). The initial HTML5 export and itch.io deploy already happened in I2-1; the theme + description upload was re-done cleanly in I2-11 (I2-6 produced the brief only; execution did not happen until I2-11). This item's deploy step is a **final redeploy** of the latest build so the live page reflects the finished tutorial before submission. Flip the itch.io page from draft to public as part of this step.
  Acceptance: Game plays in browser at the itch.io URL without download. GitHub repo is public with all code. Devpost submission is live with green "Submitted" badge, project story, screenshots, built-with tags, and both links (repo + live demo).
  Verify: Open the itch.io game page in a fresh browser tab — confirm it loads and is playable end-to-end. Open the Devpost submission page — confirm the "Submitted" badge is visible. Read the description aloud — does it land the tribute story and both wow moments?

## Iteration 3 — Nebula Background, Visual Polish, and Color Scheme

Source asset: `assets/backgrounds/nebula.jpg` (5333×3555, downloaded from Envato Elements 2026-04-23, Lifetime Commercial License via Core plan, by AveCalvar). Title at source: "Nebula Cloud Formation in Space with Stars." Cloud is dramatic teal/cyan center with coral rim, dark navy edges with stars and one prominent warm orange star bottom-right.

- [x] **I3-1. Add nebula texture to gameplay grids**
  Spec ref: `spec.md > Grid Rendering > GridRenderer` (rendering layer 1, "nebula background texture")
  What to build: In `scripts/ui/grid_renderer.gd`, add `const NEBULA_TEXTURE: Texture2D = preload("res://assets/backgrounds/nebula.jpg")` near the other constants. Replace `_draw_background()`'s flat `draw_rect` with a `draw_texture_rect_region` call that fills the full grid bounds (`Rect2(0, 0, GRID_COLS * CELL_SIZE, GRID_ROWS * CELL_SIZE)`) using a centered horizontal-band source rect chosen from the 5333×3555 source so the nebula's main mass fills the wide 4:1 grid (avoid the dark top corners and the bright bottom-right orange star). Both Command Grid and Target Grid get the texture (single insertion point, both share the renderer). Keep the existing grid lines, ships, fog-of-war, probe overlay, and reticule layers drawing on top exactly as before.
  Acceptance: Both grids render the nebula edge-to-edge with no flat-color band. Probe illumination overlay still visible on top. Grid lines still visible. No FPS regression at default zoom.
  Verify: Run the game. Reach the gameplay screen. Confirm both Command Grid and Target Grid show the nebula. Launch a probe and confirm the illumination overlay still highlights cells over the nebula. Switch tabs to confirm both grids share the same nebula source.

- [x] **I3-2. Fix wreckage z-order — destroyed ships render below live ships**
  Spec ref: `backlog.md > Bug: destroyed ships render above live ships`
  What to build: In `scripts/ui/grid_renderer.gd`, fix the draw order in `_draw_command_ships()` so wreckage markers render on a lower z-layer than living ships. Currently the loop draws wreckage and living ships in fleet order, which can put a destroyed ship above a living adjacent ship. Two-pass: first pass draws wreckage for all destroyed ships, second pass draws all living ships. Same fix likely needed for any analogous code path on the Target Grid if wreckage and FogShipRecord ships overlap there.
  Acceptance: Destroyed ships always render below living ships when they share/abut cells. No regression to normal-case rendering.
  Verify: Set up a state where a destroyed ship sits adjacent to a living ship (or use the screenshot runner if it exposes a relevant scenario). Confirm the living ship draws on top.

- [x] **I3-3. Replace flat backgrounds in static scenes**
  Spec ref: `prd.md > 9. Grid` ("Nebula background: Deep space purples, blues, teals")
  What to build: In `scenes/splash.tscn`, `scenes/main_menu.tscn`, `scenes/handoff.tscn`, and `scenes/victory.tscn`, replace the existing full-screen `ColorRect` (currently `Color(0.05, 0.05, 0.15, 1.0)`) with a `TextureRect` pointing at `res://assets/backgrounds/nebula.jpg`. Set `expand_mode = 1` (Ignore Size) and `stretch_mode = 6` (Keep Aspect Covered) so the nebula crops to fill at any window size. Keep all other UI nodes (labels, buttons, overlays) layered above. If text legibility suffers, add a semi-transparent dark `ColorRect` between the nebula and the text layer (alpha ~0.4–0.5) — judge per scene; menus likely need the dark layer for button readability, splash may not.
  Acceptance: All four static scenes show the nebula behind the UI. Text and buttons remain readable. No layout shifts.
  Verify: Run the game. Walk through splash → menu → start game → handoff → (after a turn) victory. Confirm nebula appears on every screen and text is readable. Resize the window — confirm the nebula scales without distortion.

- [x] **I3-4. Two-line title treatment on splash, main menu, and victory**
  Spec ref: `backlog.md > Two-line title treatment on splash and main menu`
  What to build: Restyle the game title from a single line to a two-line block: "Battlestations:" on the first line at the existing title size, "NEBULA" fully capitalized on the second line at a larger font size, horizontally stretched so "NEBULA" matches the pixel width of "Battlestations:" above it. Apply to `scenes/splash.tscn`, `scenes/main_menu.tscn`, and `scenes/victory.tscn` (anywhere the game title currently renders). Width matching can be done via `Label.theme_override_constants/outline_size` plus character spacing, or by placing "NEBULA" inside a `Control` with `size_flags_horizontal = SIZE_EXPAND_FILL` and scaling the label's `custom_minimum_size` to hit the target width. Use the new I3-6 palette colors once those land — for now, keep the existing title color tokens; I3-6 will sweep them.
  Acceptance: Title renders as two lines on splash, main menu, and victory. "NEBULA" is visibly larger and matches the width of "Battlestations:" above it.
  Verify: Run the game. Confirm splash, main menu, and victory each show the two-line title with "NEBULA" stretched to match "Battlestations:" width.

- [x] **I3-5. Use ship-type colors during fleet placement**
  Spec ref: `backlog.md > Ship colors during placement`
  What to build: In `scripts/fleet_placement.gd` (and `scenes/fleet_placement.tscn` if relevant), update the ship-strip rendering in the left panel and the placed-ship rendering on the placement grid to use the same `SHIP_COLORS` dictionary used by `scripts/ui/grid_renderer.gd`. Today the placement renderer draws ships in a single uniform color. After this change, each ship type (battleship, probe_ship, destroyer, cruiser) renders in its assigned color in both the strip selector and on the grid. Ghost ship (cursor preview) keeps its current red-on-invalid behavior; the base ghost color may shift toward the ship's tint for clarity.
  Acceptance: Ship strips in the left panel render in their ship-type colors. Placed ships on the grid render in their ship-type colors. Two destroyers share the destroyer color (intended — they are the same ship type). No regression in placement validation, rotation, or Done-button behavior.
  Verify: Run the game to fleet placement. Confirm the five ships in the left panel show distinct colors per ship type. Place each ship and confirm the placed sprite uses the same color. Rotate and re-place to confirm color persists.

- [x] **I3-6. Update color scheme to harmonize with the nebula**
  Spec ref: `docs/color-scheme.md` (will be rewritten)
  What to build: Sample 5–6 dominant colors from `assets/backgrounds/nebula.jpg` (teal/cyan center, coral rim, navy background, warm orange highlight, off-white star color). Define a refreshed palette and apply it across the project:
  - **Backgrounds** (where flat color still shows behind UI panels): shift from `Color(0.05, 0.05, 0.15)` to a sampled nebula-edge navy.
  - **Primary accent / titles** (currently `Color(0.4, 0.8, 1.0)` cyan): adjust toward the nebula's central teal so titles feel "of the nebula."
  - **Secondary text** (currently various blue-grays): adjust to harmonize with new primary.
  - **Warning / hit text** (currently `Color(1.0, 0.4, 0.4)` coral): align with the nebula's coral rim hue.
  - **Player accent** (currently `Color(1.0, 0.6, 0.2)` orange): keep warm but pull from the nebula's warm-star highlights.
  - **Ship colors** (`SHIP_COLORS` in `grid_renderer.gd`): only adjust if any tint disappears against the nebula's mid-tones — keep ships visually distinct from each other and from the background.
  
  Update theme overrides in `scenes/main_menu.tscn`, `scenes/splash.tscn`, `scenes/handoff.tscn`, `scenes/victory.tscn`, `scenes/gameplay.tscn`, `scenes/fleet_placement.tscn`. Rewrite `docs/color-scheme.md` to document the new palette with hex codes, source rationale (sampled from nebula or kept), and intended role per color. Note as a parking-lot item (NOT in acceptance for this checklist item): the itch.io page theme (`#0d0d26` / `#9999cc` / `#3366ff`) was set in I2-11 from the *old* palette and should be re-themed by Cowork as a follow-up if the user wants in-game and itch.io to match.
  Acceptance: All scene files use the new palette. `docs/color-scheme.md` is rewritten and accurate. Game still readable in every scene. Hit/damage indicators still register as warnings.
  Verify: Run the game. Walk through every scene. Confirm the new palette feels intentional alongside the nebula (not arbitrarily swapped) and nothing reads as broken or invisible.

- [x] **I3-7. Visual sanity pass, tuning, and screenshot regeneration**
  Spec ref: New — judgment call after the visual changes land
  What to build: Play through a full game with the nebula + new palette + two-line title + placement colors all in place. Identify and fix any of: (1) probe illumination overlay too subtle against the nebula — bump alpha or shift hue if needed, (2) grid lines invisible against bright nebula areas — bump line color brightness or alpha, (3) ship colors blending into nebula colors — adjust ship tint contrast, (4) blind hit / wreckage / ghost markers hard to read — adjust their colors or add an outline. Make minimal changes — the goal is to keep the nebula's visual impact while preserving gameplay clarity. After tuning, re-run the screenshot script (`godot --path . -- --screenshot`) to refresh `assets/screenshots/` with the new visuals. Spot-check a handful of regenerated PNGs against the descriptions in `docs/claude-cowork/screenshot-brief.md` to confirm they still match. If any drifted, fix the runner (`scripts/debug/screenshot_runner.gd`) per the CLAUDE.md "Edit the runner when..." note.
  Acceptance: All gameplay states (probe overlay, blind hit, wreckage, ghost markers, selected ship highlight) remain clearly readable over the nebula. Updated screenshots replace the old flat-background ones. The new title treatment is visible in `01_welcome.png` and `02_main_menu.png`.
  Verify: Play a partial game with the nebula in place. Confirm every visual element is legible. Open `assets/screenshots/` and confirm the new shots show the nebula, the new palette, and the two-line title.

## Iteration 4 — Viewport baseline + bug sweep

Context: `project.godot` ships with no `[display]` section, so Godot's default 1152×648 viewport + `disabled` stretch mode are in effect. Menus and gameplay were actually laid out and screenshot-verified at 1600×900 (see `scripts/debug/screenshot_runner.gd` comments). On hi-res monitors the canvas grows but UI stays pixel-fixed, so buttons and labels read as tiny. This iteration locks the base resolution to 1600×900 with `canvas_items`/`expand` stretch so the UI scales with the canvas, then clears the three live bugs, then redeploys.

- [x] **I4-1. Set project viewport to 1600×900 with canvas_items/expand stretch**
  Spec ref: New — not in original spec; fills the `[display]` gap in `project.godot`
  What to build: Add a `[display]` section to `project.godot` with `window/size/viewport_width=1600`, `window/size/viewport_height=900`, `window/stretch/mode="canvas_items"`, `window/stretch/aspect="expand"`. Reopen the project in Godot to confirm the editor picks up the new base size. Walk every scene (splash, main_menu, fleet_placement, handoff, gameplay, victory) in the editor at the new base and confirm anchors/layouts still look right — scenes were already tuned against 1600×900 (the screenshot runner's assumed window) so no layout work is expected, but if any anchor reads wrong at the real base, fix it here before moving on. Leave the web export preset's `html/canvas_resize_policy=2` (Adaptive) as-is — that's correct with `canvas_items` stretch. No changes to the screenshot runner — 1600×900 was already its assumed window, so now the project matches.
  Acceptance: `project.godot` contains the four `[display]` keys above. Running the project at the default window shows the full UI centered at 1600×900. Resizing the window larger or smaller scales the UI proportionally — buttons and text stay legible on both a small (1024×576-ish) and large (2560×1440-ish) window. No layout breakage on any scene.
  Verify: Open Godot, reopen the project. Project Settings → Display → Window → Size reads 1600×900, Stretch → Mode reads `canvas_items`, Aspect reads `expand`. F5 to run. Walk splash → menu → How to Play → Start → P1 placement → handoff → P2 placement → handoff → first gameplay turn. Grab the window corner and resize smaller and larger — confirm UI scales and stays readable. No clipped buttons, no text overflow.

- [x] **I4-2. Bug: Cruiser forward-move cost wrong after rotation**
  Spec ref: `backlog.md > Cruiser forward move cost wrong after rotation`
  What to build: During a Cruiser move action, after the player rotates with Q/E the forward-move cost discount (0.5 pts / 25 energy) should apply to the NEW facing direction. Today it keeps calculating "forward" from the pre-rotation heading, so the cheaper forward-move discount lands on the wrong direction after a rotate. Fix the move-cost calc in `scripts/gameplay/action_resolver.gd` (and/or the live preview display in `scripts/gameplay.gd`) so the forward discount tracks the ship's current facing, not its facing at the start of the move preview.
  Acceptance: Start a Cruiser move. Rotate via Q/E to change facing. Move 1 square in the NEW facing direction — live preview shows 0.5 pts / 25 energy. Move 1 square in any other direction — live preview shows 1.0 pts / 50 energy. Holds whether the rotation is the first or second move action.
  Verify: Start gameplay. Select the Cruiser. Click Move. Rotate with E (or Q). Use WASD to move 1 square in the new forward direction — confirm the cost line shows 0.5 pts / 25 energy. Back out, try a non-forward direction — confirm 1.0 / 50.

- [x] **I4-3. Bug: first-turn energy regen exceeds max_energy**
  Spec ref: `backlog.md > Bug: first-turn energy regen ignores cap`
  What to build: Fix the energy regen step in `scripts/gameplay/turn_manager.gd` (`turn_start()`) to clamp at `ship.max_energy`. Today, ships start at max_energy on fleet placement, so the +50 regen on the first turn_start pushes every ship 50 over its documented max (Battleship 1050 instead of 1000, etc.). Regen should be `energy = min(energy + 50, max_energy)`.
  Acceptance: On P1's very first turn (and P2's first turn), every ship's `energy` reads exactly its `max_energy` from ShipDefinitions — Battleship 1000, Probe Ship 1000, Destroyer 750, Destroyer 750, Cruiser 500. On subsequent turns, ships below max regen +50 but cap at max; ships already at max stay at max.
  Verify: Start a fresh game, place both fleets, reach the gameplay screen on P1's first turn. Click each of P1's five ships and read the energy value in the Ship Panel. Every value matches its documented max. End turn, hand off, check P2's ships on their first turn — same result.

- [x] **I4-4. Bug: empty-state label persists when a ship is selected**
  Spec ref: `backlog.md > Bug: empty-state label persists when ship selected`
  What to build: In `scripts/ui/ship_panel.gd`, cache a reference to the sibling `ShipPanelEmpty` label (defined in `scenes/gameplay.tscn`) during `_build_ui()`. In `show_ship()` and `show_enemy_ship()`, set the empty-state label's `visible = false` alongside making `_container.visible = true`. In `clear_ship()`, invert: empty-state label visible, container hidden. Then remove the temporary workaround in `_shot_08a_ship_panel_tight()` in `scripts/debug/screenshot_runner.gd` that hides the label manually for that one shot, and re-run the screenshot script to confirm 08a still looks clean without it.
  Acceptance: No ship selected → only the "Click a ship on the Command Grid to select it." prompt is visible. Own ship selected → only the populated panel is visible. Enemy ship viewed via probe → only the populated panel. Click away to deselect → prompt returns, panel clears. Workaround lines removed from `_shot_08a_ship_panel_tight()`. Shot 08a regenerates without the workaround and still matches the brief.
  Verify: Run the game. Reach gameplay. Confirm the empty-state prompt shows with no selection. Click one of your ships — prompt hides, panel populates. Click empty grid space — panel clears, prompt returns. Run `godot --path . -- --screenshot`. Open `assets/screenshots/08a_ship_panel_tight.png` — no empty-state text bleeding through, panel fully populated.

<!-- GATED — I4-5 drives external side effects (butler push + itch.io field edit). /build must run this item in guided-with-gates mode, matching item #12's approach. Pause for explicit user confirmation before: (a) butler push, (b) itch.io Viewport dimensions field edit. Do not flip any draft/public state — the page is already public. -->

- [x] **I4-5. Redeploy to itch.io and set Viewport dimensions field**
  Spec ref: `CLAUDE.md > Build & Deploy`; `spec.md > Runtime & Deployment`
  What to build: Guided-with-gates redeploy, per item #12's model. (1) HTML5 export via `godot --headless --path . --export-release "Web"` with threading disabled; output to `export/web/`. (2) Local smoke test on `python -m http.server 8000` — verify UI scales on resize, walk through fleet placement → gameplay, confirm the three bug fixes (cruiser forward cost, first-turn energy cap, empty-state label). Pause for user confirmation. (3) `butler push ./export/web zabuuq/battlestations-nebula:html5`. Pause for user confirmation. (4) Open the live itch.io URL in a fresh browser tab and re-verify. (5) Update the itch.io game page's "Embed options > Viewport dimensions" (or equivalent frame-size field) to `1600 × 900`. Pause for user confirmation before saving. Leave "Fullscreen button" and scrollbar settings as they were. (6) Draft a Gallows-Deadpan devlog note for Jason to post manually — per the `project_itch_devlog_on_updates.md` memory, every butler push ships with a devlog draft. Save the draft as `docs/claude-cowork/devlog-i4.md` (or append to process-notes.md, Jason's call at execution time).
  Acceptance: New butler build live at the itch.io URL. Live page's Viewport dimensions field reads 1600 × 900. UI is readable on a fresh browser tab at default size and at resized window sizes. All three bug fixes behave live. Devlog draft written and ready for manual posting.
  Verify: Open the live itch.io URL in a fresh browser tab. Resize the browser a couple of times — UI scales with it. Click through splash → menu → P1 placement → handoff → P2 placement → handoff → one full turn including a probe, a laser, and a Cruiser rotate+forward-move. Confirm energy values match documented maxes on turn 1. Confirm empty-state label behaves.

## Iteration 5 — Camera / controls rework

Context: four camera/controls items have been sitting in `backlog.md`. Bundled here because all three camera items edit the same ~30-line region in `scripts/gameplay.gd::_handle_grid_input` and pair naturally (scroll rework and mouse-centered zoom share the zoom math). The probe-outside-grid fix is targeting UX, not camera, but it's filed under controls in the backlog and is cheap enough to ride along. Original backlog entries: "Rework map scrolling and zooming controls," "Zoom centered on mouse position," "Persist map view between turns," "Probe activation outside grid."

- [x] **I5-1. Rework scroll + zoom controls with mouse-centered zoom**
  Spec ref: New — extends `spec.md > Grid Rendering > SubViewport Setup` and `spec.md > Scenes > Gameplay Screen` camera behavior; replaces the current wheel-zoom + middle-click-pan scheme.
  What to build: In `scripts/gameplay.gd::_handle_grid_input`, replace the current scheme with the following control mapping, applied to both Command Grid and Target Grid:
  - Plain scroll wheel → vertical pan (scroll up = pan camera up, scroll down = pan camera down). Pan step: ~1 cell of world distance per notch, divided by current zoom so felt pan distance stays consistent across zoom levels.
  - Shift+scroll → horizontal pan. Shift+scroll-up pans right (camera moves right), shift+scroll-down pans left (camera moves left). (Jason's convention, confirmed during scoping: scroll-up = camera up, shift+scroll-up = camera right, ctrl+scroll-up = zoom in.)
  - Ctrl+scroll → zoom in/out, centered on the current mouse position (not camera center). Use the existing 1.1 / 0.9 factor. Zoom math: compute world-space cursor position before the zoom, apply the new zoom, then shift `cam.position` so that world position is still under the cursor.
  - Left-click-drag on empty grid space → pan. Track mouse-down position and cumulative motion. If cumulative motion exceeds a 4 px threshold before mouse release, enter pan mode and suppress the select-on-release. If motion stays under the threshold, the release fires a normal click (select ship / target / deselect as today).
  - Remove all `MOUSE_BUTTON_MIDDLE` handling. `is_panning` / `pan_start_mouse` / `pan_start_cam` are retained but now driven by left-drag.
  - All pan/zoom operations still call `_clamp_camera(cam)` so the camera stays in-bounds.
  - Keep the existing `MOUSE_BUTTON_WHEEL_UP`/`DOWN` detection; just branch on `event.ctrl_pressed` and `event.shift_pressed` inside the wheel case.
  Acceptance: Plain scroll pans vertically on both grids. Shift+scroll pans horizontally. Ctrl+scroll zooms and the cell under the cursor before the zoom is still under the cursor after (pan-to-cursor behavior, verified at multiple zoom levels). Left-click-drag on empty grid space pans the camera. A quick click-release on empty space still deselects; a quick click-release on a ship still selects. A drag that starts on a ship but exceeds the 4 px threshold pans instead of selecting. Middle-click does nothing.
  Verify: Run the game. Reach gameplay. (1) Scroll up — camera pans up; scroll down — camera pans down. (2) Shift+scroll up — camera pans right; shift+scroll down — camera pans left. (3) Ctrl+scroll up — zoom in; ctrl+scroll down — zoom out. Confirm the cell under the cursor stays under the cursor across zoom. (4) Left-click-drag on empty space — camera pans, no selection occurs on release. (5) Quick click on a ship — selects. (6) Quick click on empty space — deselects. (7) Middle-click-drag — does nothing.

- [x] **I5-2. Persist camera position and zoom between turns per player**
  Spec ref: Extends `spec.md > Autoloads > GameState` (players sub-dict).
  What to build: Store per-player camera state so pan + zoom carry across turns. In `scripts/autoloads/game_state.gd`, extend each entry in `GameState.players` with two new dicts: `command_camera: Dictionary` and `target_camera: Dictionary`, each shaped `{ "position": Vector2, "zoom": float }` or empty `{}` before first save. Initialize them as empty dicts at game start. In `scripts/gameplay.gd`, save both cameras' current `position` and `zoom.x` into the current player's entry whenever the camera changes (on every pan/zoom in `_handle_grid_input`, cheap enough to write directly — no debounce needed) and whenever gameplay scene exits (`_exit_tree()` as a safety net). On `_ready()`, if the current player has non-empty saved state for either camera, restore it; otherwise fall back to the current fit-to-grid default (lines 537-541). Applies per-player — P1's and P2's saved states are independent. Reset both players' saved states to empty dicts at game start (new game from main menu) so a fresh match doesn't inherit stale camera state from a prior match. No change to spec.md needed for this iteration; if the change lands, update `spec.md > Autoloads > GameState` to document the two new fields as part of a follow-up doc pass.
  Acceptance: On P1's turn, pan + zoom on both Command Grid and Target Grid. End turn. P2's cameras load at the fit-to-grid default (first turn for P2) or P2's last saved state (subsequent turns). P2 pans + zooms independently. End turn. P1's cameras return exactly to the positions + zoom levels P1 left them at. Starting a new game from the main menu resets both players' saved states so the first gameplay scene shows fit-to-grid defaults.
  Verify: Start a game. Reach gameplay as P1. Pan Command Grid to the right edge and zoom in on a ship. Switch to Target Grid, pan to a different area, zoom differently. End turn. On P2's first gameplay turn, confirm both grids fit-to-grid. P2 pans somewhere else. End turn. P1 returns — Command Grid is at the right edge zoomed in on the same ship, Target Grid is where P1 left it. Return to main menu, start a new game — confirm fit-to-grid defaults.

- [x] **I5-3. Allow probe activation with clicks outside grid bounds**
  Spec ref: Extends `spec.md > Action System > ActionResolver > Probe Action` targeting.
  What to build: When the player is in probe-targeting mode, accept clicks that land outside the 80×20 grid and still activate the probe (the existing clamp in `ActionResolver.resolve_probe` already keeps the probe area fully within grid bounds, so no clamp work is needed — we just need to stop rejecting the click). In `scripts/gameplay.gd`, the out-of-bounds early return lives in `_handle_grid_click` at the top (`if cell.x < 0 or cell.x >= GRID_COLS ...`). Either move that bounds check below the probe-targeting branch, or split the handler so targeting clicks skip the bounds gate while selection/laser/missile clicks still enforce it. Lasers and missiles must continue to reject out-of-grid clicks (they need a real cell to target). The live reticule preview during probe targeting should already clamp to the grid visually; confirm it still does when the cursor is off-grid. No change to `ActionResolver.resolve_probe` — its internal clamp is already correct.
  Acceptance: During probe targeting on the Target Grid, clicking anywhere inside the SubViewport — including cells outside the 80×20 grid — activates a probe whose area is clamped inside the grid. Laser and missile targeting still reject out-of-grid clicks (nothing happens). Probe reticule preview stays correctly clamped when the cursor is off-grid. Ship selection clicks still reject out-of-grid clicks.
  Verify: Start a game, reach gameplay. Select a ship with a probe (e.g. Battleship). Click Probe. Move the cursor off the right edge of the 80×20 grid and click — confirm the probe fires on the rightmost clamped area. Repeat off the top/bottom/left edges. Back out, click Shoot Laser, move cursor off-grid and click — confirm nothing happens. Back out, try to click on a ship via an off-grid click — confirm nothing happens.

- [x] **I5-4. Make partially probed enemy ships clickable on all their cells**
  Spec ref: Extends `spec.md > Scenes > Gameplay Screen` (enemy ship selection) and `spec.md > Grid Rendering > GridRenderer` (Target Grid state).
  What to build: In `scripts/gameplay.gd::_try_select_enemy_ship` (lines 215-232), change the selection gate so clicking any cell of a currently-probed fog ship selects that ship, not just cells with `has_probe = true`. The current gate `record.has_probe and record.ship != null` rejects the ghost-reference cells that `ActionResolver.resolve_probe` writes for off-probe cells of a partially probed ship (see `action_resolver.gd` lines 73-82 — those cells get `record.ship` set but `has_probe` stays false). New gate: if `record.ship != null`, compute the ship's full cell set via `ShipDefinitions.get_ship_cells(record.ship.ship_type, record.ship.position, record.ship.facing)`, and scan those cells in `cell_records` to check whether any of them has `has_probe = true`. If yes, proceed with selection (same `show_enemy_ship` + `show_left_tab("ship_panel")` path as today). If no (ship is fully faded to ghost), call `_clear_enemy_selection()` as today. Cells with `ship == null` still clear selection.
  Acceptance: Launch a probe whose area covers only part of an enemy ship. The ship renders in full on the Target Grid (current behavior). Clicking any cell of that ship — including cells outside the probe area — opens the enemy ship panel. Once the probe fades and all cells are ghost-only, clicking the ship does nothing (today's behavior, preserved). Clicking an empty cell or a cell with only a ghost marker (no ship ref) still clears selection.
  Verify: Start a game, reach gameplay. Probe a partial ship. On the Target Grid, click a probed cell of the ship — panel opens (baseline). Click an unprobed cell of the same ship — panel opens (new). Wait 2-3 turns for the probe to fade to ghost. Click any cell of the now-ghost ship — nothing happens.

- [x] **I5-5. Click-to-pick-up during fleet placement**
  Spec ref: Extends `spec.md > Scenes > Fleet Placement` and `prd.md > 8. Fleet Placement Rules`.
  What to build: In `scripts/fleet_placement.gd::_on_viewport_gui_input`, add a pick-up path on left-click. Behavior: when `selected_ship_idx == -1` (nothing in hand) and the clicked grid cell overlaps an already-placed ship, pick that ship up — remove it from `placed_ships`, reset its strip-button modulate to the idle color (the opposite of line 167's `Color(0.4, 1.0, 0.4)`), set `selected_ship_idx` to its fleet index, restore `ghost_facing` from the picked-up ship's `facing`, and re-enable the Done button check at line 169. When `selected_ship_idx >= 0` (a ship is in hand), keep existing behavior — the click passes to `_try_place_ship()`, which fails silently on overlap. Helper: add `_find_placed_ship_at(cell: Vector2i) -> int` that returns the fleet index of the ship occupying `cell`, or `-1` if none. Use `ShipDefinitions.get_ship_cells(stype, position, facing)` the same way `_is_placement_valid` does at line 172-182. Priority order on a left-click with no ship in hand: pick-up check first, then the existing no-op path.
  Acceptance: Place all 5 ships. With nothing in hand, left-click any placed ship — that ship is picked back up (strip button returns to idle color, ghost follows cursor with the same facing it had when placed, Done button becomes disabled). Re-place the ship at a new spot — placement works normally. With a ship currently in hand, left-click on a placed ship — nothing happens (current overlap-invalid behavior preserved). Right-click still cancels the current placement.
  Verify: Start a new game, reach fleet placement. Place all 5 ships. Click Done is enabled. Click one placed ship — confirm it's picked up, Done is now disabled, ghost follows cursor. Place it somewhere new — Done re-enables. Start placing a different ship, then try clicking a placed ship — confirm nothing happens. Right-click to cancel — confirm current placement clears.

- [x] **I5-6. Stay on Target Grid after probe/laser/missile, auto-switch left panel to Battle Log**
  Spec ref: Extends `spec.md > Scenes > Gameplay Screen` (post-action flow) and `spec.md > Battle Log`.
  What to build: In `scripts/gameplay.gd::_execute_targeting_action` (lines 299-339), remove the `_switch_grid(ActiveGrid.COMMAND)` call at line 331 so the Target Grid stays active after a probe/laser/missile resolves. Replace it with `_show_left_tab("battle_log")` so the player sees the just-logged result without having to switch tabs manually. Keep `_cancel_targeting()` (clears targeting state) and the two `renderer.refresh()` calls. Remove the `ship_panel.show_ship(selected_ship)` block at lines 337-339 only if it's load-bearing after the left-tab switch — more likely, leave it so the Ship Panel stays populated for when the player clicks back to that tab; only the visible left tab changes. No change to the Move action's post-resolve flow (move doesn't target the enemy grid, so staying on Command Grid after a move is still correct).
  Acceptance: After launching a probe, firing a laser, or firing a missile, the active grid stays on Target Grid and the left panel shows the Battle Log with the fresh entry. The player can still click Command Grid / Target Grid tab buttons to switch manually. Clicking a ship still switches left panel to Ship Panel as today. After a Move action, nothing changes — Command Grid stays active, left panel behavior unchanged.
  Verify: Start a game, reach gameplay. Select a ship, click Probe, click the Target Grid — confirm the grid tab auto-switches to Target (baseline). Click a target cell — confirm probe resolves, grid stays on Target Grid, left panel shows the Battle Log with the new entry. Repeat for Laser and Missile. Make a Move action — confirm grid stays on Command Grid and left panel behavior is unchanged.
